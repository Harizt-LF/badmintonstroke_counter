import 'package:flutter/material.dart';

class ConnectBle extends StatelessWidget {
  const ConnectBle({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
